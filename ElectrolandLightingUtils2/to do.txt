0.) replace param names with CONSTANTS
1.) replace all variable names with CONSTANTS in ARTNetRecipient
2.) replace object types with CONSTANTS in option error messages
2.) Recipient.sync allOn allOff
3.) ELUCanvas.sync & allOn, allOff
4.) ElectrolandLightingManager.run and allOn, allOff
3.) ARTNetRecipient.send
5.) EluCanvas2D.sync
5.) change "-" to something else so we can do negative numbers.
5.) finish validation TODOs in ELUManager
6.) basic GUI (including sweep, etc.)
8.) test
9.) clean up

GUI
+-----------------------------------------+
|[ - select canvas - ]                    | <-- drop down  
+-----------------------------------------+
+  show detectors           | meta-data   +
+                           | for         +
+                           | selected    +
+                           | detector    +
+                           | and fixtre  +
+                           |             +
+                           |             +
+                           |             +
+                           |             +
+-----------------------------------------+
+ desired fps [    ] measured fps: 33     + <-- text field
+ [sweep] [alloff] [allon] [start] [stop] + <-- buttons
+-----------------------------------------+

When a detector is selected, highlight it, medium light the other detectors in
the same fixture as it.  Spit out meta-data on the right side, including
what DetectionModel is defined for it, plus geomotry and fixturetype.

Color of each detector should reflect current state.
